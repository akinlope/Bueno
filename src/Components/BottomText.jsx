const BottomText = ({ text }) => {
  return (
    <div className="text-center mt-16">
      <p className="capitalize text-priBrown opacity-40">{text}</p>
    </div>
  );
};

export default BottomText;
